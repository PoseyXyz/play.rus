import React from 'react';
import { Link } from 'react-router-dom';

function Nav(props) {
    return (
        <div>
            <Link to='xbox'>NAVBAR</Link>
        </div>
    );
}

export default Nav;